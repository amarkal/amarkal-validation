To run all tests in this folder, use the following command from within the `tests` folder:

```
$ phpunit .
```

To run a specific test suite, for example `StringsTest.php`, call:

```
$ phpunit StringsTest.php
```